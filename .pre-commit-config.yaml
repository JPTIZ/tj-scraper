repos:
  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: poetry run mypy --ignore-missing-imports
        language: system
        types: [python]
      - id: black
        name: black
        entry: poetry run black --check --diff
        language: system
        types: [python]
